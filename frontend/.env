REACT_APP_BACKEND_URL=http://127.0.0.1:8000
# WDS_SOCKET_PORT defaults to the dev server port (3000).  Set only behind a reverse proxy on a different port.
# WDS_SOCKET_PORT=443
ENABLE_HEALTH_CHECK=false